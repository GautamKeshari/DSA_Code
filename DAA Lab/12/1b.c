#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>
#include<time.h>

#define MAX_SIZE 100

struct Pair{
    int x, y;
};

struct Bhulbhulaiya{
    int m,n;
    bool H[MAX_SIZE][MAX_SIZE];
    bool V[MAX_SIZE][MAX_SIZE];
    struct Pair P[MAX_SIZE][MAX_SIZE];
};

struct Bhulbhulaiya initbhul(int n,int m){
    
};

int main(){
    int n,m;
    scanf("%d %d",&n,&m);
    int H[m-1][n]; 
    int V[m-1][n];
     
}