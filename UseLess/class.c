#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
#include<windows.h>

int main() {
    int i;
    while((i<=2) || (!CreateProcess))
    {
        i++;
    }
    printf("Hello\n");
    for(;;);

    return 0;
}